apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota
  namespace: flask-app
spec:
  hard:
    pods: "10"
    requests.cpu: "4"
    requests.memory: "16Gi"
    limits.cpu: "8"
    limits.memory: "32Gi"